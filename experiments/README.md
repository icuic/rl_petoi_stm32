# experiments

实验记录目录，只保留当前主线里程碑和可复现结论。早期探索记录已从 Git 中移除，生成的评估 JSON 默认不纳入 Git 跟踪。

- `petoi_bittle_v0_trot_residual_v3_phase_fixed_100k_continue.md`：记录 full-state v3 phase-fixed 策略继续训练 100k 后的最佳前进基线。
- `petoi_bittle_v0_demo_milestone.md`：记录当前 Petoi locomotion demo、ONNX 导出和可复现命令。
